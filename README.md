# python-docker
Python Installing using docker and executing the simple python file using docker 


Steps:
1) Git clone 
2) Check if the docker is installed else then install sudo snap install docker  sudo apt  install docker.io   sudo apt  install docker-compose
3) In the python docker folder run docker-compose up -d 
4) Enable the 5000 port of the localhost.
